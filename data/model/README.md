# Neural ordering model

Place the trained `othello_ordering.model` in this directory, then set
`neural_ordering_enabled=true` in `data/config/game.ini`.

Model files are generated locally and are not committed to Git.
